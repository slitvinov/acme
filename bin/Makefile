M = ac add com cx gp gn gd gui lsort m mac mouse new p pp pull push py Run to ws
PREFIX = $(HOME)
B = $(PREFIX)/bin

all:
	mkdir -p $B && \
	for i in $M; \
	do \
		cp $$i $B; \
		chmod +x $B/$$i; \
	done