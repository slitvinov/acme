HOME=/u
GIT_PAGER=cat
PATH=/u/acme/bin:/u/plan9port/bin:/u/.local/bin:$PATH

export GIT_PAGER HOME
unset SHELL

mkdir -p /tmp/ns.$USER.:0

font=Ubuntu
fontsrv -p . 2>/dev/null | /usr/bin/grep -qx $font/ || font=Menlo-Regular

plumber
exec acme -f /mnt/font/$font/15a/font /u/acme/a "$@"
